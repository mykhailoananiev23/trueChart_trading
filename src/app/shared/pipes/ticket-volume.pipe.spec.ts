import { TicketVolumePipe } from './ticket-volume.pipe';

describe('TicketVolumePipe', () => {
  it('create an instance', () => {
    const pipe = new TicketVolumePipe();
    expect(pipe).toBeTruthy();
  });
});
